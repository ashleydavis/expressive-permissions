# Step 7: (removed)

This step previously described built-in TypeScript permission rules, then a default `permissions.yaml`. Both have been removed. The plugin ships with no default rules — users define all permission policy in their own `.claude/permissions.yaml`.

No files to create. No action required.
