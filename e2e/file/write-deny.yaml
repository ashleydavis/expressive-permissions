description: "file: write to /etc/** deny"

input:
  tool_name: Write
  tool_input:
    file_path: /etc/hosts
    content: "127.0.0.1 evil.com"
  cwd: /home/user/project

rules:
  write:
    path: "/etc/**"
    decide: deny
    reason: "system files denied"

expected:
  decision: deny
  reason: "system files denied"
