description: "bash: custom command with no descriptor; unknown flag defaults to arity-0, no rule matches, decision is ask"

input:
  tool_name: Bash
  tool_input:
    command: "mytool --output result.txt"
  cwd: "${PROJECT_DIR}"

rules: {}

expected:
  decision: ask
