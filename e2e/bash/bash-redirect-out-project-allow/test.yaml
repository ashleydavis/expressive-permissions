description: "redirect.out: allow write under project dir"

input:
  tool_name: Bash
  tool_input:
    command: "echo hi > ./logs/out.txt"
  cwd: ${PROJECT_DIR}

rules:
  redirect:
    out:
      - path-in: ["./**"]
        decide: allow
      - decide: ask

expected:
  decision: allow
