description: "bash not: env does not match → rule fires (deny)"

input:
  tool_name: Bash
  tool_input:
    command: "AWS_PROFILE=prod aws ec2 delete-vpc --vpc-id vpc-12345"
  cwd: /home/user/project

rules:
  bash:
    aws:
      decide: deny
      not:
        env:
          AWS_PROFILE: sandbox

expected:
  decision: deny
