description: "bash: ls path | grep pattern both allow"

input:
  tool_name: Bash
  tool_input:
    command: "ls /home/blah/projects/expressive-permissions/e2e/bash/ | grep -i git"
  cwd: /home/blah/projects/expressive-permissions

rules:
  bash:
    ls:
      decide: allow
    grep:
      decide: allow

expected:
  decision: allow
