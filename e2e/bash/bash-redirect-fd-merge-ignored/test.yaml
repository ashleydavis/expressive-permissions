description: "redirect.out: fd merge ignored, file target matched"

input:
  tool_name: Bash
  tool_input:
    command: "cmd > /tmp/out 2>&1"
  cwd: ${PROJECT_DIR}

rules:
  redirect:
    out:
      - path-in: ["/tmp/**"]
        decide: allow
      - decide: ask

expected:
  decision: allow
