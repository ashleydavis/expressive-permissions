description: "bash file: contains matches → ALLOW"

input:
  tool_name: Bash
  tool_input:
    command: "kubectl delete pod mypod"
  cwd: /home/user/project

rules:
  bash:
    kubectl:
      file:
        fixtures/sandbox.txt:
          contains: "sandbox"
      decide: allow

expected:
  decision: allow
