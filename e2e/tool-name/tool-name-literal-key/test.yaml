description: "tool-name rule: literal key matches Grep"

input:
  tool_name: Grep
  tool_input:
    pattern: "TODO"
  cwd: /home/user/project

rules:
  Grep:
    decide: allow

expected:
  decision: allow
