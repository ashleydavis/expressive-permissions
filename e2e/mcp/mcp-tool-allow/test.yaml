description: "mcp: tool glob allow"

input:
  tool_name: mcp__my_server__search
  tool_input:
    query: "foo"
  cwd: /home/user/project

rules:
  mcp:
    tool: "mcp__my_server__*"
    decide: allow

expected:
  decision: allow
