description: "mcp: tool-in OR list allow"

input:
  tool_name: mcp__my_server__search
  tool_input:
    query: "foo"
  cwd: /home/user/project

rules:
  mcp:
    tool-in:
      - "mcp__my_server__search"
      - "mcp__my_server__fetch"
    decide: allow

expected:
  decision: allow
