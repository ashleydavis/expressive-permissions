description: "both configs loaded simultaneously: rule from home config allows cat"
source_e2e: e2e/integration/home-and-project-home-rule/test.yaml
input:
  tool_name: Bash
  tool_input:
    command: cat /etc/hosts
  cwd: /home/user/project
rules:
  bash:
    ls:
      decide: allow
expected:
  decision: allow
home_rules:
  bash:
    cat:
      decide: allow
