description: "integration: export FOO=bar; cmd — export visible downstream"
source_e2e: e2e/integration/export-sequence/test.yaml
input:
  tool_name: Bash
  tool_input:
    command: export FOO=bar; ls
  cwd: /home/user/project
rules:
  bash:
    export:
      decide: allow
    ls:
      env:
        FOO: bar
      decide: allow
expected:
  decision: allow
