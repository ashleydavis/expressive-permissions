description: "bash: cmd1 && cmd2 both allow → allow"
source_e2e: e2e/bash/bash-and-both-allow/test.yaml
input:
  tool_name: Bash
  tool_input:
    command: ls && pwd
  cwd: /home/user/project
rules:
  bash:
    ls:
      decide: allow
    pwd:
      decide: allow
expected:
  decision: allow
