description: "bash: ask rule with reason"
source_e2e: e2e/bash/bash-ask-by-binary/test.yaml
input:
  tool_name: Bash
  tool_input:
    command: curl https://example.com
  cwd: /home/user/project
rules:
  bash:
    curl:
      decide: ask
      reason: network access requires approval
expected:
  decision: ask
  reason: network access requires approval
