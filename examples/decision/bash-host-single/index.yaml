description: "bash: curl URL positional glob match"
source_e2e: e2e/bash/bash-host-single/test.yaml
input:
  tool_name: Bash
  tool_input:
    command: curl https://api.example.com/data
  cwd: /home/user/project
rules:
  bash:
    curl:
      cmd: "https://api.example.com/*"
      decide: allow
expected:
  decision: allow
