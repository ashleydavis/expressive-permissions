description: "integration: FOO=bar; cmd — standalone assignment visible downstream"
source_e2e: e2e/integration/env-set-sequence/test.yaml
input:
  tool_name: Bash
  tool_input:
    command: FOO=bar; ls
  cwd: /home/user/project
rules:
  bash:
    "":
      decide: allow
    ls:
      env:
        FOO: bar
      decide: allow
expected:
  decision: allow
