description: "webfetch: omit host and host-in to match every WebFetch call"
input:
  tool_name: WebFetch
  tool_input:
    url: https://example.com/data
  cwd: ${PROJECT_DIR}
rules:
  webfetch:
    decide: allow
    reason: Allow fetching any URL
expected:
  decision: allow
  reason: Allow fetching any URL
