tool_call:
  tool_name: Bash
  tool_input:
    command: rm --recursive --force /tmp
ast:
  type: bash
  source: rm --recursive --force /tmp
  children:
    command:
      type: command
      source: rm --recursive --force /tmp
      commandName: rm
      options:
        recursive: true
        force: true
      positionals:
        - /tmp
      envPrefix: {}
