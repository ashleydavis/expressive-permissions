tool_call:
  tool_name: Bash
  tool_input:
    command: |-
      cd /app
      npm install
      npm test
ast:
  type: bash
  raw: |-
    cd /app
    npm install
    npm test
  ast:
    type: binop
    op: ;
    left:
      type: binop
      op: ;
      left:
        type: command
        binary: cd
        options: {}
        cmd: /app
        envPrefix: {}
        raw: cd /app
      right:
        type: command
        binary: npm
        options: {}
        cmd: install
        envPrefix: {}
        raw: npm install
    right:
      type: command
      binary: npm
      options: {}
      cmd: test
      envPrefix: {}
      raw: npm test
