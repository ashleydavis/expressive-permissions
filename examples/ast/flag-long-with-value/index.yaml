tool_call:
  tool_name: Bash
  tool_input:
    command: git push --remote=origin
ast:
  type: bash
  source: git push --remote=origin
  children:
    command:
      type: command
      source: git push --remote=origin
      commandName: git
      options:
        remote: origin
      positionals:
        - push
      envPrefix: {}
