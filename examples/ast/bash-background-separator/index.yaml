tool_call:
  tool_name: Bash
  tool_input:
    command: npm run dev & npm run watch
ast:
  type: bash
  raw: npm run dev & npm run watch
  ast:
    type: binop
    op: ;
    left:
      type: command
      binary: npm
      options: {}
      cmd:
        - run
        - dev
      envPrefix: {}
      raw: npm run dev
    right:
      type: command
      binary: npm
      options: {}
      cmd:
        - run
        - watch
      envPrefix: {}
      raw: npm run watch
