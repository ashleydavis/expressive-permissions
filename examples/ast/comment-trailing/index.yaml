tool_call:
  tool_name: Bash
  tool_input:
    command: "ls -la # list the directory"
ast:
  type: bash
  source: "ls -la # list the directory"
  children:
    command:
      type: command
      source: "ls -la # list the directory"
      commandName: ls
      options:
        l: true
        a: true
      positionals: []
      envPrefix: {}
