tool_call:
  tool_name: Bash
  tool_input:
    command: kubectl delete --context prod-cluster
ast:
  type: bash
  source: kubectl delete --context prod-cluster
  children:
    command:
      type: command
      source: kubectl delete --context prod-cluster
      commandName: kubectl
      options:
        context: prod-cluster
      positionals:
        - delete
      envPrefix: {}
descriptors:
  kubectl:
    description: Kubernetes CLI
    flags:
      context:
        arity: 1
        kind: string
        description: Kubernetes context name
