tool_call:
  tool_name: Bash
  tool_input:
    command: "cd /tmp && rm -rf *"
ast:
  type: bash
  raw: "cd /tmp && rm -rf *"
  ast:
    type: binop
    op: "&&"
    left:
      type: command
      binary: cd
      options: {}
      pos: /tmp
      envPrefix: {}
      redirects: []
      raw: "cd /tmp"
    right:
      type: command
      binary: rm
      options:
        r: true
        f: true
      pos: "*"
      envPrefix: {}
      redirects: []
      raw: "rm -rf *"
