tool_call:
  tool_name: Bash
  tool_input:
    command: git push --force-with-lease
ast:
  type: bash
  source: git push --force-with-lease
  children:
    command:
      type: command
      source: git push --force-with-lease
      commandName: git
      options:
        force-with-lease: true
      positionals:
        - push
      envPrefix: {}
