tool_call:
  tool_name: Bash
  tool_input:
    command: "rm --recursive --force /tmp"
ast:
  type: bash
  raw: "rm --recursive --force /tmp"
  ast:
    type: command
    binary: rm
    options:
      recursive: true
      force: /tmp
    cmd: []
    envPrefix: {}
    redirects: []
    raw: "rm --recursive --force /tmp"
