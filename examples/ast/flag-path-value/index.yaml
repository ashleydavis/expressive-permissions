tool_call:
  tool_name: Bash
  tool_input:
    command: git -C /some/path status -sb
ast:
  type: bash
  source: git -C /some/path status -sb
  children:
    command:
      type: command
      source: git -C /some/path status -sb
      commandName: git
      options:
        C: /some/path
        s: true
        b: true
      positionals:
        - status
      envPrefix: {}
descriptors:
  git:
    description: Git
    flags:
      C:
        arity: 1
        kind: path
        description: Run as if started in the given directory
