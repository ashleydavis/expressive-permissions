tool_call:
  tool_name: Bash
  tool_input:
    command: git commit -m wip
ast:
  type: bash
  source: git commit -m wip
  children:
    command:
      type: command
      source: git commit -m wip
      commandName: git
      options:
        m: wip
      positionals:
        - commit
      envPrefix: {}
descriptors:
  git:
    description: Git
    flags:
      m:
        arity: 1
        kind: string
        description: Commit message
